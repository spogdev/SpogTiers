// Deliberately empty. The root project on this branch is a container: the backend
// subproject declares its own plugins, and nothing here should pull in Fabric Loom
// or Minecraft, which the service does not use.
