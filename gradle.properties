# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Fabric Properties — https://fabricmc.net/develop
# NOTE: Minecraft 26.x ships DEOBFUSCATED. Yarn stopped at 1.21.11 and the
# intermediary for this version is an empty 0.0.0 stub, so this branch builds
# against official Mojang mappings instead. See PORTING.md.
minecraft_version=26.1.2
loader_version=0.19.3
loom_version=1.18.0-alpha.16

# Mod Properties
mod_version=0.1.6
maven_group=com.spog
archives_base_name=spogtiers

# Dependencies
fabric_version=0.155.2+26.1.2
modmenu_version=18.0.0
