# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# Backend properties. Versioned independently of the mod: the service and the
# mod are deployed separately and do not have to move in lockstep.
backend_version=0.1.0
resolver_version=0.1.0
maven_group=com.spog
